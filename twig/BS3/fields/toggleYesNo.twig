{% macro render(field) %}
    <div>
        <div class="btn-group" id="{{ field.id }}" data-toggle="buttons">
            <label class="btn btn-default yes {{ (field.value=='Y' ? 'active' : '') }}">
                <input type="radio" class="{{ field.class }}" name="{{ field.name }}" value="Y" autocomplete="off" {{ (field.value=='Y' ? 'checked="checked"' : '') }} {{ field.getDataAttributeString()|raw }}> Yes
            </label>
            <label class="btn btn-default no {{ (field.value=='N' ? 'active' : '') }}">
                <input type="radio" class="{{ field.class }}" name="{{ field.name }}" value="N" autocomplete="off" {{ (field.value=='N' ? 'checked="checked"' : '') }} {{ field.getDataAttributeString()|raw }}> No
            </label>
        </div>
    </div>
    {% if field.getScript() is not empty %}
        <script type="text/javascript">{{ field.getScript()|raw }}</script>
    {% endif %}
{% endmacro %}
