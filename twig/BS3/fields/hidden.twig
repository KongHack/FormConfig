{% macro render(field) %}
    <input type="hidden" id="{{ field.id }}" name="{{ field.name }}" class="{{ field.class }}" value="{{ field.value }}" {{ field.getDataAttributeString()|raw }}>
{% endmacro %}