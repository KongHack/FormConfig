{% macro render(field) %}
        <ion-input
            clear-input
            id="{{ field.getName() }}"
            value="{{ field.getValue() }}"
            maxlength="{{ field.getMaxLength() }}">
        </ion-input>
{% endmacro %}