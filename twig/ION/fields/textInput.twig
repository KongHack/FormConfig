{% extends '@form_config_ION/field_wrapper.twig' %}
{% block textPosition %}
position="floating"
{% endblock %}
{% block inputContent %}
    <ion-input
        clear-input
        id="{{ id }}"
        value="{{ value }}"
        maxlength="{{ maxLength }}"
        {{ disabled ? 'disabled' : '' }}>
    </ion-input>
{% endblock %}