{% macro render(field) %}
    <ion-input
        clearInput
        id="{{ field.getName() }}"
        value="{{ field.getValue() }}"
        maxlength="{{ field.getMaxLength() }}"
        type="password">
    </ion-input>
{% endmacro %}