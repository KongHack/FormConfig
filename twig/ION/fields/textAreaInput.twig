{% extends '@form_config_ION/field_wrapper.twig' %}
{% block textPosition %}
position="floating"
{% endblock %}
{% block inputContent %}
    <ion-textarea
        id="{{ id }}"
        value="{{ value }}"
        maxlength="{{ maxLength }}"
        rows="4"
        {{ disabled ? 'disabled' : '' }}>
    </ion-textarea>
{% endblock %}