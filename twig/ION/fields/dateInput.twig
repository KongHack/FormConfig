{% extends '@form_config_ION/field_wrapper.twig' %}
{% block inputContent %}
    <ion-datetime
        id="{{ id }}"
        value="{{ value }}"
        max="{{ "now"|date('Y') + 40 }}"
        display-format="{{ dateDisplayFormat }}"
        {{ disabled ? 'disabled' : '' }}>
    </ion-datetime>
{% endblock %}
